// import { NextApiRequest, NextApiResponse } from 'next';

// // It should NOT be a react component / Neither return (because it's a BACKNED logic for API, not a Page)

// export default function (req, res) {

//   // res.json({ status: 'ok' });
//   res.json({ num: Math.floor(Math.random() * 10) });
// }


// // http://localhost:3000/api/<file-name>